# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'livereload' do
#  watch(%r{css/views/.+\.(erb|haml|slim)$})
#  watch(%r{app/helpers/.+\.rb})
  watch(%r{/.+\.(css|js|html)})
  watch(%r{.+\.html})
  # Rails Assets Pipeline
#  watch(%r{(app|vendor)/assets/\w+/(.+\.(css|js|html)).*})  { |m| "/assets/#{m[2]}" }
end
